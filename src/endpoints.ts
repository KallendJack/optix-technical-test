const BASE_URL = 'http://localhost:3000'

export const endpoints = {
    movies: `${BASE_URL}/movies`,
    movieCompanies: `${BASE_URL}/movieCompanies`,
    submitReview: `${BASE_URL}/submitReview`,
}
